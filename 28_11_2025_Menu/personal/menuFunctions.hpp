#pragma once

#include "menuStructures.hpp"

namespace tkr{
	const MenuItem* showMenu1(const MenuItem*);
	const MenuItem* showMenu2(const MenuItem*);
	const MenuItem* showMenu3(const MenuItem*);

	const MenuItem* exit(const MenuItem*);
	const MenuItem* goBack(const MenuItem*);

	const MenuItem* spbuPMPU(const MenuItem*);
	const MenuItem* spbuMatMech(const MenuItem*);
	const MenuItem* spbuLing(const MenuItem*);
	
	const MenuItem* unisSPbU(const MenuItem*);
	const MenuItem* unisITMO(const MenuItem*);
	const MenuItem* unisPoly(const MenuItem*);
	const MenuItem* unisLETI(const MenuItem*);
}
